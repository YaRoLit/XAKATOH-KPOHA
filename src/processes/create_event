# Процесс для создания события

def create_event(tgbot, call):
    #try:
    markup = types.InlineKeyboardMarkup(row_width=2)
    
    markup.add(market, wallet)
                                                                                                                                                # Нереализованный функционал Веб-приложения
                                                                                                                                                #web_app_info = types.WebAppInfo("https://localhost:5000")
                                                                                                                                                #button = types.KeyboardButton(text="Веб-приложение", web_app=web_app_info)
                                                                                                                                                #markup.add(button)
    #except Exception as e:
        #print(e)
    try:
        tgbot.edit_message_text(chat_id=call.message.chat.id, message_id=call.message.message_id, text=f"Выберите дату:", reply_markup=markup)
    except:
        main = tgbot.send_message(call.message.chat.id, f"Выберите дату:", reply_markup=markup)